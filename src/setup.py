from setuptools import setup 

setup(
    name='Second-Brain-Tools',
    version = '0.0.1',
    description = 'This is a module set for all the python utils that are being used on Second brain project and its subsequent Children.',
    py_modules = ['__init__.py'],
    package_dir ={ '' : 'src'},
)